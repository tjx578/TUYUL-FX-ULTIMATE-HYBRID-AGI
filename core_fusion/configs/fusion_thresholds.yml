# ==========================================================
# ⚙️ Fusion Threshold Configuration — TUYUL FX v5.8r++
# Layer: Fusion Spectre → TRQ–3D → Reflective
# Integrasi ke: Fusion Precision, Equilibrium Fusion, FRPC Engine
# ==========================================================

fusion_thresholds:
  # Ambang deviasi VWAP (untuk Smart Money Counter)
  vwap_deviation_factor: 1.2        # ATR multiplier (default: 1.2)
  spread_threshold: 55              # Min. spread MFI–CCI50 untuk valid reversal zone

  # Energi reflektif minimum
  reflective_energy_min: 0.85       # Minimal energi TRQ–3D agar valid
  reflective_energy_max: 1.5        # Batas maksimal resonansi

  # Confidence level minimal untuk valid signal
  reflective_confidence_min: 0.75
  reflective_confidence_max: 0.98

  # FRPC (Fusion Reflective Propagation Coefficient)
  frpc_stable_min: 0.93             # Minimal nilai FRPC untuk dianggap sinkron
  frpc_drift_limit: 0.80            # Jika <0.8 → masuk mode resinkronisasi

  # Integritas Vault minimal
  integrity_index_min: 0.95         # Nilai minimal vault untuk operasi normal

meta:
  version: 6.0
  build: "reflective-production"
  updated: "2026-01-03T15:00:00Z"
  vault_layer: "Fusion Spectre Layer"
  description: "Ambang batas utama untuk deteksi, refleksi, dan sinkronisasi lintas layer."
