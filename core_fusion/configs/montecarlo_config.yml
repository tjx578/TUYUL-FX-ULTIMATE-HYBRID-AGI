# ==========================================================
# 🎲 Monte Carlo Simulation Config — TUYUL FX v5.8r++
# Layer: Fusion Spectre → Meta Learning
# Integrasi ke: MonteCarlo Validator, Reflective Trade Plan Generator
# ==========================================================

montecarlo:
  iterations: 20000                 # Jumlah simulasi per siklus
  lookback_days: 90                 # Periode data untuk estimasi probabilitas
  confidence_level: 0.95            # Confidence interval untuk validasi reflektif
  risk_per_trade: 0.01              # Risiko per posisi (1%)
  rr_target: 1.8                    # Target Reward:Risk Ratio
  profit_factor_min: 1.4            # Minimum profit factor untuk valid setup
  win_rate_threshold: 0.62          # Minimal probabilitas kemenangan

reflective_parameters:
  sync_with_trq3d: true
  sync_with_rgo: true
  adaptive_drift_compensation: true # Menyesuaikan Monte Carlo hasil REE feedback
  reflective_weight: 0.85           # Bobot reflektif untuk hasil simulasi (vs. statistik mentah)

meta:
  version: 6.0
  build: "reflective-production"
  updated: "2026-01-03T15:00:00Z"
  vault_layer: "Fusion Spectre Layer"
  description: "Parameter simulasi Monte Carlo untuk validasi probabilitas reflektif."
